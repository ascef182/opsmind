# dependencies
node_modules/
.pnpm-store/

# build output
dist/
build/
.next/
.turbo/
*.tsbuildinfo

# env
.env
.env.local
.env.*.local
!.env.example

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# editor
.vscode/*
!.vscode/extensions.json
.idea/

# os
.DS_Store

# testing
coverage/
apps/api/eval-report.json

# prisma
packages/database/prisma/dev.db

# Isolated worktrees for subagent-driven-development
.worktrees/

# Scratch workspace (ledger, briefs, review packages) for subagent-driven-development
.superpowers/

# uploads locais (LocalStorageService — Fase 4, STORAGE_PROVIDER=local)
.data/

# terraform (Fase 6) — state e vars têm segredos/IPs reais, nunca versionados.
# .terraform.lock.hcl NÃO entra aqui — esse sim é pra versionar (fixa as
# versões de provider, garante reprodutibilidade entre máquinas).
infra/terraform/.terraform/
infra/terraform/terraform.tfstate
infra/terraform/terraform.tfstate.backup
infra/terraform/terraform.tfvars
infra/terraform/*.tfplan
