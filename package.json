{
  "name": "opsmind",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "dotenv -e .env -- turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:e2e": "dotenv -e .env -- turbo run test:e2e",
    "db:migrate": "dotenv -e .env -- pnpm --filter @opsmind/database run migrate",
    "db:studio": "dotenv -e .env -- pnpm --filter @opsmind/database exec prisma studio",
    "seed": "dotenv -e .env -- pnpm --filter @opsmind/database run seed"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "turbo": "^2.1.3",
    "typescript": "^5.6.3"
  }
}
